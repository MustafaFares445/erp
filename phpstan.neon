includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: max

    paths:
        - app
        - bootstrap/app.php
        - config
        - database
        - routes

    tmpDir: storage/framework/cache/phpstan
